const fieldTypes = {
    name: 'name',
    email: 'email',
    count: 'count',
    price: 'price'
};

export const fieldTypesArray = [
    fieldTypes.name,
    fieldTypes.email,
    fieldTypes.count,
    fieldTypes.price
];

export default fieldTypes;
